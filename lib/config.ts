// Where the service is mounted on the host.
// Must stay in sync with `basePath` in next.config.ts and the path
// matchers in Caddyfile.
//
// To deploy at the apex (e.g. on a dedicated subdomain), set this to "".
export const BASE_PATH = "/pdf_comp";
